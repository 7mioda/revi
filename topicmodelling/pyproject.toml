[project]
name = "topicmodelling"
version = "0.1.0"
description = "Categorize PR comments using topic modelling"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "turftopic",
    "sentence-transformers",
    "numpy",
]
